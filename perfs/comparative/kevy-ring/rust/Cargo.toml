[workspace]

[package]
name = "kevy-ring-comparative-bench"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
kevy-ring = { path = "../../../../crates/kevy-ring" }
rtrb = "0.3"
ringbuf = "0.4"
crossbeam-queue = "0.3"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
