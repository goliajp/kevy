# Verb reference

Every wire-reachable verb, from the server's own metadata table
(`COMMAND DOCS` answers from the same rows). GENERATED by
`cargo run -p kevy --bin gen_docs .` — do not edit by hand.

184 verbs. Flags: `write`/`readonly` (side-effect class),
`admin`, `blocking`, `pubsub`, `transaction`, `extension`
(kevy-specific surface; argument 1 is a catalog name, not a key).

**Complexity** is the cost of THIS engine's implementation, read out of the
code — not copied from Redis's reference. Several genuinely differ, and they
look like typos and are not: `ZRANK` is O(N) because our sorted set has no
rank-augmented structure, `ZCOUNT` and `ZRANGEBYSCORE` scan rather than seek,
and every geo search is O(N).

**Redis compatibility** is `full`, `differs: …`, or `kevy-only`. This is the
column to read before a migration; it is also the column Redis's own reference
cannot have.

## scan

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `KEYS pattern` | 2 | readonly | O(N) keys in the whole keyspace — every shard walks its entire map, plus an O(P*K) glob match per key | full | Return every key matching the glob pattern (cross-shard gather). |
| `RANDOMKEY` | 1 | readonly | O(1) expected — each shard probes a random slot, and the origin folds the candidates through a reservoir weighted by shard key counts, so every key is equally likely | full | Return a random key from the keyspace. |
| `SCAN cursor [MATCH pattern] [COUNT count]` | -2 | readonly | O(N) keys in the WHOLE keyspace, on every call — there is no incremental walk | differs: SCAN is not a cursor iterator. Every call sweeps every shard's entire keyspace and replies with cursor 0 and all matches in one batch, so the standard SCAN loop terminates after one round trip. COUNT and TYPE are accepted and ignored; MATCH is honoured. Redis's incremental, bounded-work, rehash-tolerant guarantees do not exist here | Sweep the keyspace and return every match at once; NOT a cursor iterator (COUNT and TYPE are accepted and ignored, and the cursor is always 0). |

## generic

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `DEL key [key ...]` | -2 | write | O(N) keys, plus O(M) per key holding a collection of M elements (the drop walks the container) | differs: the multi-key form is split by shard and summed with no barrier, so it is not atomic across shards; the single-key form is | Delete one or more keys. |
| `EXISTS key [key ...]` | -2 | readonly | O(N) keys, one O(1) probe each | differs: the multi-key form is gathered per shard, so the count is not a point-in-time snapshot across shards | Count how many of the given keys exist. |
| `EXPIRE key seconds` | 3 | write | O(1) | differs: the NX / XX / GT / LT condition flags are not parsed — arity is fixed at 3, so EXPIRE key seconds NX is a wrong-arity error | Set a key's TTL in seconds (non-positive TTL deletes the key). |
| `EXPIREAT key unix-time-seconds` | 3 | write | O(1) | differs: the NX / XX / GT / LT condition flags are not parsed — arity is fixed at 3 | Set a key's expiry as an absolute Unix timestamp in seconds. |
| `PERSIST key` | 2 | write | O(1) | full | Remove a key's TTL. |
| `PEXPIRE key milliseconds` | 3 | write | O(1) | differs: the NX / XX / GT / LT condition flags are not parsed — arity is fixed at 3 | Set a key's TTL in milliseconds. |
| `PEXPIREAT key unix-time-milliseconds` | 3 | write | O(1) | differs: the NX / XX / GT / LT condition flags are not parsed — arity is fixed at 3 | Set a key's expiry as an absolute Unix timestamp in milliseconds. |
| `PTTL key` | 2 | readonly | O(1) | full | Return a key's remaining TTL in milliseconds (-1 no TTL, -2 missing). |
| `RENAME key newkey` | 3 | write | O(1) for a string value; O(M) for a collection of M elements (the entry's weight is re-derived by walking the value) | differs: a cross-shard rename is a non-atomic Take then Put — there is a window in which neither key is visible; co-locate the two keys with a {hashtag} for the atomic path | Rename a key, overwriting the destination (write routed at the runtime Op level). |
| `RENAMENX key newkey` | 3 | write | O(1) for a string value; O(M) for a collection of M elements | differs: cross-shard RENAMENX is a non-atomic Take, Put, restore-on-refusal sequence — there is a window in which the source is absent from the keyspace | Rename a key only if the destination does not exist (write routed at the runtime Op level). |
| `TTL key` | 2 | readonly | O(1) | full | Return a key's remaining TTL in seconds (-1 no TTL, -2 missing). |
| `TYPE key` | 2 | readonly | O(1) | full | Return the type of the value stored at key. |
| `UNLINK key [key ...]` | -2 | write | O(N) keys, plus O(M) per collection-typed key — identical to DEL | differs: UNLINK is an alias of DEL — the value is freed inline on the owning shard's reactor thread, not by a background thread, so it gives no latency benefit | Delete one or more keys (alias of DEL in kevy's shard model). |

## string

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `APPEND key value` | 3 | write | O(1) amortised while the value is <= 64 B; O(N) per call once it exceeds that (the Arc payload is copied out and re-boxed on every append), so building a large string by repeated APPEND is O(N^2) | full | Append bytes to a string value; returns the new length. |
| `DECR key` | 2 | write | O(1) | full | Decrement the integer value of a key by one. |
| `DECRBY key decrement` | 3 | write | O(1) | full | Decrement the integer value of a key by the given amount. |
| `GET key` | 2 | readonly | O(1) | full | Return the string value of a key. |
| `GETDEL key` | 2 | write | O(1) | full | Return the string value of a key and delete it. |
| `GETSET key value` | 3 | write | O(1) | full | Set a key's string value and return its previous value. |
| `INCR key` | 2 | write | O(1) | full | Increment the integer value of a key by one. |
| `INCRBY key increment` | 3 | write | O(1) | full | Increment the integer value of a key by the given amount. |
| `INCRBYFLOAT key increment` | 3 | write | O(1) | differs: arithmetic is f64 and the reply uses Rust's shortest-round-trip float printer, not a long double accumulator, so trailing digits can diverge from Redis | Increment the float value of a key by the given amount. |
| `MGET key [key ...]` | -2 | readonly | O(N) keys, one O(1) probe each on the owning shard | differs: keys are grouped by shard and each shard is probed independently, so the reply is not a point-in-time snapshot across shards | Return the values of multiple keys (cross-shard gather). |
| `MSET key value [key value ...]` | -3 | write | O(N) pairs, one O(1) SET each | differs: atomic only per shard — pairs are split by key and applied shard-by-shard with no cross-shard barrier, so a reader can observe some pairs written and others not; Redis MSET is globally atomic | Set multiple key/value pairs atomically per shard. |
| `PSETEX key milliseconds value` | 4 | write | O(1) | full | Set a key's value with a TTL in milliseconds. |
| `SET key value [EX seconds\|PX milliseconds] [NX\|XX]` | -3 | write | O(1) | differs: only NX / XX / EX / PX are parsed — KEEPTTL, EXAT, PXAT, GET and the IFEQ family are a syntax error; a repeated expire option is silently accepted last-one-wins instead of erroring | Set a key's string value with optional TTL and existence conditions. |
| `SETEX key seconds value` | 4 | write | O(1) | full | Set a key's value with a TTL in seconds. |
| `SETNX key value` | 3 | write | O(1) | full | Set a key's value only if it does not exist. |
| `STRLEN key` | 2 | readonly | O(1) | full | Return the length of a key's string value. |

## hash

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `HDEL key field [field ...]` | -3 | write | O(M) fields | full | Delete one or more hash fields. |
| `HEXISTS key field` | 3 | readonly | O(1) | full | Check whether a hash field exists. |
| `HEXPIRE key seconds [NX\|XX\|GT\|LT] FIELDS numfields field [field ...]` | -6 | write | O(F) fields in the FIELDS clause | differs: the FIELDS parse error reads 'Mandatory keyword FIELDS is missing' where Redis says 'Mandatory argument FIELDS' | Set per-field TTLs on a hash, in seconds. |
| `HGET key field` | 3 | readonly | O(1) | full | Return the value of a hash field. |
| `HGETALL key` | 2 | readonly | O(N) fields, with a full copy of every name and value | full | Return all fields and values of a hash. |
| `HINCRBY key field increment` | 4 | write | O(1) | full | Increment the integer value of a hash field. |
| `HKEYS key` | 2 | readonly | O(N) fields | full | Return all field names of a hash. |
| `HLEN key` | 2 | readonly | O(1) | full | Return the number of fields in a hash. |
| `HMGET key field [field ...]` | -3 | readonly | O(M) fields requested | full | Return the values of multiple hash fields. |
| `HMSET key field value [field value ...]` | -4 | write | O(M) pairs | full | Deprecated alias of HSET that replies OK. |
| `HPERSIST key FIELDS numfields field [field ...]` | -5 | write | O(F) fields | differs: silently accepts a stray NX\|XX\|GT\|LT token before FIELDS (it shares HEXPIRE's parser) where Redis rejects it | Remove per-field TTLs from a hash. |
| `HPEXPIRE key milliseconds [NX\|XX\|GT\|LT] FIELDS numfields field [field ...]` | -6 | write | O(F) fields | differs: same FIELDS error-text deviation as HEXPIRE | Set per-field TTLs on a hash, in milliseconds. |
| `HPEXPIREAT key unix-time-milliseconds [NX\|XX\|GT\|LT] FIELDS numfields field [field ...]` | -6 | write | O(F) fields | differs: same FIELDS error-text deviation as HEXPIRE | Set per-field expiries as absolute Unix-millisecond deadlines. |
| `HSCAN key cursor [MATCH pattern] [COUNT count]` | -3 | readonly | O(N) — the whole hash is copied and returned in one batch | differs: not a cursor iterator — the cursor is always 0 and the entire hash comes back in a single reply; NOVALUES is not parsed and COUNT is validated then ignored | Iterate a hash's fields and values (single-batch cursor). |
| `HSET key field value [field value ...]` | -4 | write | O(M) pairs | full | Set one or more hash fields; returns the number of new fields. |
| `HSETNX key field value` | 4 | write | O(1) | full | Set a hash field only if it does not exist. |
| `HPTTL key FIELDS numfields field [field ...]` | -5 | readonly | O(F) fields | differs: silently accepts a stray NX\|XX\|GT\|LT token before FIELDS where Redis rejects it | Return per-field remaining TTLs in milliseconds (-1 no TTL, -2 missing). |
| `HTTL key FIELDS numfields field [field ...]` | -5 | readonly | O(F) fields | differs: silently accepts a stray NX\|XX\|GT\|LT token before FIELDS where Redis rejects it | Return per-field remaining TTLs in seconds (-1 no TTL, -2 missing). |
| `HVALS key` | 2 | readonly | O(N) fields | full | Return all values of a hash. |

## list

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `BLPOP key [key ...] timeout` | -3 | write,blocking | O(1) to serve; the park is O(1) and waiters are woken FIFO per key | differs: on a multi-shard server the multi-key form does not honour Redis's left-to-right key priority — keys on the connection's own shard are served before remote ones | Blocking left pop across one or more lists (effect logged as LPOP). |
| `BRPOP key [key ...] timeout` | -3 | write,blocking | O(1) to serve; FIFO park | differs: same multi-key priority deviation as BLPOP | Blocking right pop across one or more lists (effect logged as RPOP). |
| `BRPOPLPUSH source destination timeout` | 4 | write,blocking | O(1) to serve | differs: a cross-shard move is not atomic — the element is popped on the source's shard and pushed on the destination's, and between those two steps it is in neither list. Co-locate the keys with a {hashtag} to get Redis's atomic path | Blocking RPOPLPUSH: pop the source tail and push it onto the destination head. |
| `LINDEX key index` | 3 | readonly | O(1) — the list is a VecDeque, so an index is a direct offset (Redis's quicklist is O(N)) | full | Return the element at the given list index. |
| `LLEN key` | 2 | readonly | O(1) | full | Return the length of a list. |
| `LMOVE source destination LEFT\|RIGHT LEFT\|RIGHT` | 5 | write | O(1) | differs: a cross-shard move is not atomic — see BRPOPLPUSH | Atomically move an element between two lists. |
| `LPOP key [count]` | -2 | write | O(C) elements popped | differs: LPOP key 0 on an existing key replies a nil array where Redis replies an empty array | Pop one or more elements from the head of a list. |
| `LPOS key element [RANK rank] [COUNT num-matches] [MAXLEN len]` | -3 | readonly | O(N) always — the whole list is copied before RANK / COUNT / MAXLEN are applied, so MAXLEN bounds the comparisons but not the copy | full | Return the index of matching elements in a list. |
| `LPUSH key element [element ...]` | -3 | write | O(M) values, O(1) amortised each | full | Prepend one or more elements to a list. |
| `LRANGE key start stop` | 4 | readonly | O(M) elements returned — the start offset is skipped in O(1) (Redis is O(S+M)) | full | Return a range of list elements. |
| `LREM key count element` | 4 | write | O(N*R) worst case — the scan is O(N) but each removal shifts up to half the deque (Redis is O(N+R)) | full | Remove elements equal to the given value from a list. |
| `LSET key index element` | 4 | write | O(1) — a direct index write (Redis is O(N) for a mid-list index) | full | Set the list element at the given index. |
| `LTRIM key start stop` | 4 | write | O(R) elements removed | full | Trim a list to the given range. |
| `RPOP key [count]` | -2 | write | O(C) elements popped | differs: RPOP key 0 on an existing key replies a nil array where Redis replies an empty array | Pop one or more elements from the tail of a list. |
| `RPOPLPUSH source destination` | 3 | write | O(1) | differs: a cross-shard move is not atomic — see BRPOPLPUSH | Pop the source tail and push it onto the destination head. |
| `RPUSH key element [element ...]` | -3 | write | O(M) values, O(1) amortised each | full | Append one or more elements to a list. |

## set

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `SADD key member [member ...]` | -3 | write | O(M) members | full | Add one or more members to a set. |
| `SCARD key` | 2 | readonly | O(1) | full | Return the number of members in a set. |
| `SDIFF key [key ...]` | -2 | readonly | O(sum of the source cardinalities) | full | Return the difference of the given sets (cross-shard gather). |
| `SDIFFSTORE destination key [key ...]` | -3 | write | O(sum of the source cardinalities) + O(result) to write the destination on its own shard | full | Store the difference of the given sets into destination. |
| `SINTER key [key ...]` | -2 | readonly | O(sum of the source cardinalities) — there is NO smallest-set-first ordering, so SINTER huge tiny costs O(huge) where Redis costs O(tiny * k) | full | Return the intersection of the given sets (cross-shard gather). |
| `SINTERSTORE destination key [key ...]` | -3 | write | O(sum of the source cardinalities) + O(result); same no-smallest-first algorithm | full | Store the intersection of the given sets into destination. |
| `SISMEMBER key member` | 3 | readonly | O(1) | full | Check whether a value is a member of a set. |
| `SMEMBERS key` | 2 | readonly | O(N) members | full | Return all members of a set. |
| `SPOP key [count]` | -2 | write | O(count) expected — each member is drawn by probing a random slot and taking the first occupied one | full | Remove and return one or more random members of a set. |
| `SRANDMEMBER key [count]` | -3 | readonly | O(count) expected when count is a small fraction of the set (random-slot probing); O(N) once count exceeds a quarter of it, where copying and shuffling beats rejection sampling | full | Return one or more random members of a set without removing them. A negative count allows repeats. |
| `SREM key member [member ...]` | -3 | write | O(M) members | full | Remove one or more members from a set. |
| `SSCAN key cursor [MATCH pattern] [COUNT count]` | -3 | readonly | O(N) — the whole set is copied and returned in one batch | differs: not a cursor iterator — the cursor is always 0 and the entire set comes back in one reply; COUNT is validated then ignored | Iterate a set's members (single-batch cursor). |
| `SUNION key [key ...]` | -2 | readonly | O(sum of the source cardinalities) | full | Return the union of the given sets (cross-shard gather). |
| `SUNIONSTORE destination key [key ...]` | -3 | write | O(sum of the source cardinalities) + O(result) | full | Store the union of the given sets into destination. |

## zset

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `BZPOPMIN key [key ...] timeout` | -3 | write,blocking | O(log N) per pop; the park is O(1) | full | Blocking pop of the lowest-scored member across one or more sorted sets. |
| `ZADD key [NX\|XX] [GT\|LT] [CH] [INCR] score member [score member ...]` | -4 | write | O(M log N) — O(1) hash insert plus O(log N) tree insert per member | full | Add members with scores to a sorted set, with conditional flags. |
| `ZCARD key` | 2 | readonly | O(1) | full | Return the number of members in a sorted set. |
| `ZCOUNT key min max` | 4 | readonly | O(N) — a full scan and filter, NOT a range seek; Redis's O(log N) does not apply | full | Count members with scores within the given bounds. |
| `ZDIFFSTORE destination numkeys key [key ...]` | -4 | write | O(sum of the source cardinalities + R log R); each source is extracted whole on its own shard | full | Store the difference of the given sorted sets into destination. |
| `ZINCRBY key increment member` | 4 | write | O(log N) | full | Increment a member's score in a sorted set. |
| `ZINTERCARD numkeys key [key ...] [LIMIT limit]` | -3 | readonly | O(sum of the source cardinalities) — LIMIT does not short-circuit the gather, only the combine | full | Return the cardinality of the intersection of the given sorted sets. |
| `ZINTERSTORE destination numkeys key [key ...] [WEIGHTS weight [weight ...]] [AGGREGATE SUM\|MIN\|MAX]` | -4 | write | O(sum of the source cardinalities + R log R) | full | Store the intersection of the given sorted sets into destination. |
| `ZPOPMIN key [count]` | -2 | write | O(M log N) | full | Pop up to count members with the lowest scores. |
| `ZPOPMIN.BELOW key below [count]` | -3 | write,extension | O(M log N) — take_while stops at the first score at or above the threshold, so it never walks past the due prefix | kevy-only: a delayed-job primitive (score = due time); no Redis analogue | kevy extension: pop up to count lowest members with score strictly below the threshold (delayed-job primitive). |
| `ZRANGE key start stop [WITHSCORES]` | -4 | readonly | O(start + M) — the tree iterator advances one node at a time; there is no rank index to jump with | differs: only the legacy 'key start stop [WITHSCORES]' form is accepted — the Redis 6.2 BYSCORE / BYLEX / REV / LIMIT arguments are a syntax error | Return members by rank range. |
| `ZRANGEBYSCORE key min max [WITHSCORES] [LIMIT offset count]` | -4 | readonly | O(N + M) — a full scan and filter; LIMIT is applied after the whole match set is materialised, so it does not reduce the work | full | Return members with scores within the given bounds. |
| `ZRANK key member` | 3 | readonly | O(N) — a linear position scan. NOT O(log N): our zset has no rank-augmented structure | differs: the Redis 7.2 WITHSCORE option is not supported | Return a member's rank, ordered from the lowest score. |
| `ZREM key member [member ...]` | -3 | write | O(M log N) | full | Remove one or more members from a sorted set. |
| `ZREMRANGEBYRANK key start stop` | 4 | write | O(start + M log N) | full | Remove members within the given rank range. |
| `ZREMRANGEBYSCORE key min max` | 4 | write | O(N + M log N) — a full scan to find the hit set, then M deletes | full | Remove members with scores within the given bounds. |
| `ZREVRANGEBYSCORE key max min [WITHSCORES] [LIMIT offset count]` | -4 | readonly | O(N + M) — a forward full scan and filter, then the result is reversed | full | Return members with scores within the given bounds, highest first. |
| `ZSCAN key cursor [MATCH pattern] [COUNT count]` | -3 | readonly | O(N) — the whole set is materialised | differs: not a cursor iterator — every call returns the whole set with cursor 0, and COUNT is parsed then ignored | Iterate a sorted set's members and scores (single-batch cursor). |
| `ZSCORE key member` | 3 | readonly | O(1) — a hash lookup | full | Return a member's score. |
| `ZUNIONSTORE destination numkeys key [key ...] [WEIGHTS weight [weight ...]] [AGGREGATE SUM\|MIN\|MAX]` | -4 | write | O(sum of the source cardinalities + R log R) | full | Store the union of the given sorted sets into destination. |

## stream

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `XACK key group id [id ...]` | -4 | write | O(A log P) | full | Acknowledge pending entries for a consumer group. |
| `XADD key [NOMKSTREAM] [MAXLEN [=\|~] threshold \| MINID [=\|~] id] id\|* field value [field value ...]` | -5 | write | O(log N) to insert; O(N*F) whenever a MAXLEN / MINID clause actually evicts, because the trim triggers a full weight recompute | differs: the approximate-trim '~' is accepted for wire compatibility but always trims exactly, and the LIMIT clause is not parsed | Append an entry to a stream, with optional trim. |
| `XAUTOCLAIM key group consumer min-idle-time start [COUNT count] [JUSTID]` | -6 | write | O(log P + K + COUNT log N); the idle filter runs before the take, so a PEL of fresh entries is walked in full | differs: the next-cursor is last-claimed+1 rather than 0-0 when the PEL scan completes, so a client looping until 0-0 needs one extra round trip | Scan and claim idle pending entries starting from a cursor. |
| `XCLAIM key group consumer min-idle-time id [id ...] [IDLE ms] [TIME unix-time-milliseconds] [RETRYCOUNT count] [FORCE] [JUSTID]` | -6 | write | O(A * (log P + log N)) | differs: IDLE / TIME / RETRYCOUNT / FORCE / JUSTID are parsed but LASTID is not, and a missing key or group returns 'no such key' instead of NOGROUP | Claim specific idle pending entries for a consumer. |
| `XDEL key id [id ...]` | -3 | write | O(D log N + N*F) for D ids — NOT O(1): the trailing weight recompute is linear in the whole stream | full | Delete entries from a stream by ID. |
| `XGROUP CREATE key group id\|$ [MKSTREAM] \| DESTROY key group \| SETID key group id\|$ \| CREATECONSUMER key group consumer \| DELCONSUMER key group consumer` | -2 | write | CREATE / DESTROY: O(1) plus an O(N*F) reweigh. SETID / CREATECONSUMER: O(1). DELCONSUMER: O(P) | differs: CREATE / SETID / DESTROY / CREATECONSUMER / DELCONSUMER / MKSTREAM are supported, but on a missing key the non-CREATE subcommands return 0 or NOGROUP instead of Redis's error, and XGROUP HELP is absent | Manage stream consumer groups. |
| `XINFO STREAM key \| GROUPS key \| CONSUMERS key group \| HELP` | -2 | readonly | STREAM: O(1). GROUPS: O(G*N) — 'lag' is a linear walk of every entry, once per group. CONSUMERS: O(C) | differs: STREAM / GROUPS / CONSUMERS / HELP only — XINFO STREAM FULL is absent; radix-tree-keys and nodes are synthetic (we have no radix tree), entries-read is approximated, and CONSUMERS omits 'inactive' | Introspect a stream, its groups, or its consumers. |
| `XLEN key` | 2 | readonly | O(1) | full | Return the number of entries in a stream. |
| `XPENDING key group [[IDLE min-idle-time] start end count [consumer]]` | -3 | readonly | summary: O(P*C) — the per-consumer tally is a linear find per PEL row. extended: O(log P + K) | full | Inspect pending entries of a consumer group (summary or extended form). |
| `XRANGE key start end [COUNT count]` | -4 | readonly | O(log N + M) — a B-tree range descent, COUNT applied lazily | full | Return stream entries within an ID range. |
| `XREAD [COUNT count] [BLOCK milliseconds] STREAMS key [key ...] id [id ...]` | -4 | readonly,blocking | O(S * (log N + M)) over S streams; '$' is O(1); the park is O(1) | differs: the '+' last-id form is not parsed — only explicit IDs and '$' are accepted | Read new entries from one or more streams, optionally blocking. |
| `XREADGROUP GROUP group consumer [COUNT count] [BLOCK milliseconds] [NOACK] STREAMS key [key ...] id [id ...]` | -7 | write,blocking | '>' form: O(log N + U + M) — COUNT does NOT bound the scan, the entire undelivered tail is materialised first. Replay form: O(log P + K) | differs: COUNT is parsed but does not bound the '>' scan | Read stream entries as a consumer-group member, optionally blocking. |
| `XREVRANGE key end start [COUNT count]` | -4 | readonly | O(log N + M) | full | Return stream entries within an ID range, in reverse order. |
| `XSETID key last-id [ENTRIESADDED entries-added] [MAXDELETEDID max-deleted-id]` | -3 | write | O(log N) | full | Overwrite a stream's last-generated ID and bookkeeping counters. |
| `XTRIM key MAXLEN\|MINID [=\|~] threshold` | -4 | write | O(R log N + N*F) for R evicted entries | differs: '~' is accepted but trimming is always exact, LIMIT is not parsed, and trailing tokens after the threshold are silently ignored instead of erroring | Trim a stream by length or minimum ID. |

## geo

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `GEOADD key [NX\|XX] [CH] longitude latitude member [longitude latitude member ...]` | -5 | write | O(M log N); CH adds an O(M^2) term | full | Add geospatial members (longitude/latitude) to a geo set. |
| `GEODIST key member1 member2 [m\|km\|mi\|ft]` | -4 | readonly | O(1) | full | Return the distance between two geo members. |
| `GEOHASH key member [member ...]` | -3 | readonly | O(M) | full | Return the base32 geohash string of each member. |
| `GEOPOS key member [member ...]` | -3 | readonly | O(M) | full | Return the longitude/latitude of each member. |
| `GEORADIUS key longitude latitude radius m\|km\|mi\|ft [WITHCOORD] [WITHDIST] [WITHHASH] [COUNT count [ANY]] [ASC\|DESC] [STORE key] [STOREDIST key]` | -6 | write | O(N) per call; STORE / STOREDIST add O(K log K) to write plus O(D) to clear the destination | differs: the destination is written on its own shard, so a STORE across shards is not atomic with the search | Legacy radius search around a coordinate, with optional STORE writes. |
| `GEORADIUSBYMEMBER key member radius m\|km\|mi\|ft [WITHCOORD] [WITHDIST] [WITHHASH] [COUNT count [ANY]] [ASC\|DESC] [STORE key] [STOREDIST key]` | -5 | write | O(N) per call; the anchor lookup is O(1) | differs: the destination is written on its own shard, so a STORE across shards is not atomic with the search | Legacy radius search around an existing member, with optional STORE writes. |
| `GEOSEARCH key FROMMEMBER member\|FROMLONLAT longitude latitude BYRADIUS radius m\|km\|mi\|ft\|BYBOX width height m\|km\|mi\|ft [ASC\|DESC] [COUNT count [ANY]] [WITHCOORD] [WITHDIST] [WITHHASH]` | -4 | readonly | O(N) per call — up to nine neighbour ranges, each fetched with a full zset scan; the geohash block prunes distance maths, not iteration | differs: STOREDIST is accepted and ignored on the read-only form; COUNT 0 is an error | Search members within a radius or box from a member or coordinate. |
| `GEOSEARCHSTORE destination source FROMMEMBER member\|FROMLONLAT longitude latitude BYRADIUS radius m\|km\|mi\|ft\|BYBOX width height m\|km\|mi\|ft [ASC\|DESC] [COUNT count [ANY]] [STOREDIST]` | -5 | write | O(N) to search, plus O(D + K log K) to rewrite the destination on its own shard | differs: STOREDIST stores the distance in the unit the query asked for (Redis does the same); the destination is written on its own shard, so the source and destination may live on different cores and the write is not atomic with the read | Run GEOSEARCH on source and store the hits into destination. |

## connection

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `ECHO message` | 2 | readonly | O(1) | full | Return the given message. |
| `HELLO [protover]` | -1 | readonly | O(1) | differs: only HELLO [2\|3] is honoured — the AUTH and SETNAME tails are parsed off and ignored, because kevy has no AUTH and this form does not set the connection name | Handshake: report server info and switch RESP protocol version (2 or 3). |
| `PING [message]` | -1 | readonly | O(1) | differs: kevy has no RESP2 subscriber mode, so PING while subscribed does not switch to the array form and non-pubsub commands stay legal on a subscribed connection | Ping the server; returns PONG or echoes the optional message. |
| `QUIT` | 1 | readonly | O(1) | full | Close the connection after replying OK. |
| `SELECT index` | 2 | readonly | O(1) | differs: kevy is single-DB — SELECT 0 replies OK and any other index is an error | Select the logical database; kevy is single-DB and only accepts index 0. |

## server

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `BGREWRITEAOF` | 1 | write,admin | O(S) fan-out; each shard freezes a copy-on-write view and hands serialise plus fsync to its own worker | differs: replies +OK rather than Redis's status line, rewrites one AOF per shard, and replies +OK even when AOF is disabled | Start an append-only-file rewrite in the background. |
| `BGSAVE` | 1 | write,admin | O(S) fan-out; O(N/S) copy-on-write freeze per shard on the reactor, then serialise off-thread | differs: replies +OK rather than Redis's status line, and writes one dump per shard | Start a snapshot save in the background. |
| `COMMAND [COUNT \| LIST \| INFO command [command ...] \| DOCS [command [command ...]]]` | -1 | readonly | bare / LIST / DOCS: O(V) verbs. COUNT: O(1). INFO / DOCS with k names: O(k*V) — the lookup is a linear scan | differs: COUNT / LIST / INFO / DOCS only — GETKEYS and GETKEYSANDFLAGS are absent, and the info rows hard-code first/last/step and leave ACL categories, tips and key-specs empty | Introspect the command table (COUNT, LIST, INFO, DOCS subcommands). |
| `CONFIG GET parameter \| SET parameter value \| REWRITE \| RESETSTAT` | -2 | write,admin | GET: O(patterns * 16). SET: O(1) plus one config swap that shards pick up on their next tick. REWRITE: O(config file size) | differs: 16 Redis-named parameters exist; only maxmemory, maxmemory-policy, appendfsync, the auto-rewrite pair, hz, maxmemory-samples, loglevel and logfile are hot-settable — bind, port, io-threads, dir and appendonly error at runtime; RESETSTAT is a no-op | Read or change server configuration parameters. |
| `CLIENT ID \| GETNAME \| SETNAME name \| LIST \| KILL filter \| INFO \| NO-EVICT ON\|OFF` | -2 | readonly,admin | ID / GETNAME / SETNAME / INFO: O(1). LIST: O(S) fan-out + O(C) rows. KILL: O(S) + O(C) scan | differs: ID / GETNAME / SETNAME / INFO / LIST (bare) / KILL (ID, ADDR) / NO-EVICT only; CLIENT LIST with a filter is a syntax error and every other subcommand (PAUSE, REPLY, TRACKING, UNBLOCK, NO-TOUCH) is unknown | Connection introspection and control subcommands. |
| `CLUSTER INFO \| NODES \| SLOTS \| SHARDS \| KEYSLOT key \| COUNTKEYSINSLOT slot` | -2 | readonly,admin | INFO / NODES / SLOTS / SHARDS / MYID: O(S). KEYSLOT: O(len(key)). COUNTKEYSINSLOT: O(N/S) | differs: a read-only virtual-node topology derived from config (one virtual master per shard) — no gossip, no MEET / FORGET / SETSLOT effect (they reply OK as no-ops), no MIGRATE / ASK, no failover; COUNTKEYSINSLOT counts only the answering shard's keys | Cluster topology introspection (virtual-node model over shards). |
| `DBSIZE` | 1 | readonly | O(S) — one message per shard, each answering O(1) from its map length; NOT O(N) | full | Return the number of keys across all shards. |
| `DEBUG SLEEP seconds \| subcommand [args ...]` | -2 | readonly,admin | SLEEP: parks the ANSWERING shard's reactor thread for the duration (other shards keep serving). Everything else: O(1) | differs: SLEEP is the only subcommand with an effect and it blocks one shard, not the server; every other subcommand is accepted and answered OK without doing anything | Debug subcommands; SLEEP blocks the shard, other subcommands are tolerated as OK. |
| `FLUSHALL` | -1 | write | O(S) fan-out; per shard O(N/S) clear plus WATCH invalidation plus a feed-generation bump | differs: ASYNC / SYNC are accepted and ignored (the flush is always synchronous per shard), and it bumps the CDC feed generation, so live FEED cursors get a resync error | Delete every key on every shard. |
| `FLUSHDB` | -1 | write | O(N + S) — identical to FLUSHALL | differs: kevy is single-DB, so FLUSHDB is an alias of FLUSHALL; ASYNC / SYNC are ignored | Delete every key of the current database (kevy is single-DB: same as FLUSHALL). |
| `INFO [section]` | -1 | readonly | O(1) in the keyspace — it never walks it; O(S) to sum the per-shard gauges | differs: 8 sections only (server, clients, memory, persistence, stats, replication, cluster, keyspace) — no commandstats / latencystats / errorstats / cpu; reports redis_version 7.4.0 for client sniffing plus a kevy_version line, and the values are up to one tick stale | Report server statistics and status sections as text. |
| `MEMORY USAGE key [SAMPLES count] \| STATS \| DOCTOR \| PURGE \| MALLOC-STATS` | -2 | readonly | USAGE: O(1) — the accounting is exact, not sampled. STATS: O(1) from the instance-wide gauges | differs: USAGE's SAMPLES count is parsed and ignored (our accounting is exact); PURGE is a no-op (system allocator, no arena); DOCTOR and MALLOC-STATS return canned strings; STATS is a trimmed 8-field set | Memory introspection subcommands. |
| `SAVE` | 1 | write,admin | O(S) fan-out; O(N/S) copy-on-write freeze per shard | differs: SAVE is no longer synchronous-until-durable — it freezes a copy-on-write view and replies OK immediately, so 'SAVE returned, therefore the dump is safe to copy' no longer holds | Synchronously snapshot the dataset to disk. |
| `SHUTDOWN [NOSAVE\|SAVE]` | -1 | write,admin | O(1) to trip the stop flag; the drain that follows is O(N) | differs: only SHUTDOWN [NOSAVE\|SAVE] parses — NOW, FORCE and ABORT are a syntax error | Stop the server process (connection drops without a reply). |
| `SLOWLOG GET [count] \| LEN \| RESET \| HELP` | -2 | readonly,admin | GET: O(S) fan-out plus O(L log L) to merge. LEN: O(S). RESET: O(S) | differs: the log is per-shard and GET merges across shards by timestamp, so ids are not globally monotonic; every entry's client-addr and client-name are empty strings | Inspect or reset the slow-command log. |

## replication

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `FAILOVER host port [TIMEOUT ms] \| ABORT` | -3 | write,admin | O(1) at the command — the handover runs in the background, polling the target until it is caught up | differs: Redis's syntax is FAILOVER [TO host port [FORCE]] [ABORT] [TIMEOUT ms] with an optional target; kevy's is FAILOVER host port [TIMEOUT ms] \| ABORT — the target is mandatory and positional, and the port given is the target's client port | Planned zero-loss handover: quiesce writes, wait for the target replica to drain, promote it, and follow it. |
| `REPL.TOKEN` | 1 | readonly,extension | O(S) — one (generation, offset) pair per shard | kevy-only: the read-your-writes token; the nearest Redis concept is master_repl_offset, which is not a resumable per-shard cursor | kevy extension: mint a read-your-writes token — per-shard [generation, offset] pairs (a primary reports its live feed tail; a replica its applied positions). |
| `REPL.WAIT gen offset [gen offset ...] [TIMEOUT milliseconds]` | -3 | readonly,blocking,extension | O(S) — a barrier, not a keyspace op; the wall-clock cost is the replication lag, bounded by TIMEOUT (default 1s, hard-capped at 60s) | kevy-only: waits until a (generation, offset) token has been applied, which is what makes read-your-writes possible across a failover | kevy extension: on a replica, block until every shard has applied the given REPL.TOKEN (then reads are read-your-writes); +OK on success, -MISDIRECTED writer is <primary> on timeout or generation mismatch. Default TIMEOUT 1000 ms, hard cap 60 s. On a primary: immediate +OK. |
| `REPLICAOF host port \| NO ONE` | 3 | write,admin | O(1) at the command; the full resync that follows is O(N) in the background | differs: the port argument is the primary's REPLICATION port base, not its client port (the default base is client port + 10000); chain replication is rejected | Make this server a replica of another, or promote it with NO ONE. |
| `ROLE` | 1 | readonly | O(1) on a replica; O(R) on a primary | full | Report the replication role and state of this server. |
| `SLAVEOF host port \| NO ONE` | 3 | write,admin | O(1) plus a background resync | differs: a deprecated alias of REPLICAOF, including the replication-port-base argument | Legacy alias of REPLICAOF. |
| `WAIT numreplicas timeout` | 3 | readonly,blocking | O(S) — an all-shard barrier returning the MINIMUM acked-replica count; the wall-clock cost is the replication round trip | differs: it is an all-shard barrier returning the minimum across shards (Redis has one offset to wait on); timeout 0 is capped at 60s rather than blocking forever, and an ACK means the frame reached the replica's apply pipeline, not fsync durability | Block until every shard's master_repl_offset is acknowledged by at least numreplicas replicas (or timeout ms pass); returns the minimum acked-replica count across shards. timeout 0 = wait forever, hard-capped at 60 s. NOTE: a replica ACK is not fsync durability. |

## tx

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `DISCARD` | 1 | readonly,transaction | O(1) — drops the queued commands and the WATCH set | full | Abort the open MULTI transaction. |
| `EXEC` | 1 | readonly,transaction | O(sum of the queued commands' costs); with W watched keys, one extra O(W) pre-check fanned into O(min(W,S)) messages | differs: a MULTI batch spanning several shards is per-shard-atomic, not globally isolated (there is no cross-shard snapshot); a queue-time error aborts the whole batch with EXECABORT, but a runtime error inside EXEC leaves the other commands applied, as in Redis | Execute every command queued since MULTI. |
| `MULTI` | 1 | readonly,transaction | O(1) | full | Start a transaction; subsequent commands are queued until EXEC. |
| `UNWATCH` | 1 | readonly,transaction | O(1) | full | Forget all watched keys. |
| `WATCH key [key ...]` | -2 | readonly,transaction | O(W) keys, grouped into O(min(W,S)) cross-shard messages | full | Watch keys for optimistic locking of the next EXEC. |

## pubsub

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `PSUBSCRIBE pattern [pattern ...]` | -2 | readonly,pubsub | O(p * P) — each pattern costs a linear scan of the shared pattern registry | full | Subscribe to channels matching the given glob patterns. |
| `PUBLISH channel message` | 3 | readonly,pubsub | O(1) channel lookup + O(P) — a linear walk of the registered patterns with one glob match each, even when none match. Delivery fans out only to the shards holding a subscriber | full | Post a message to a channel; returns the receiver count. |
| `PUNSUBSCRIBE [pattern [pattern ...]]` | -1 | readonly,pubsub | O(p * P) | full | Unsubscribe from patterns (all patterns when none given). |
| `SUBSCRIBE channel [channel ...]` | -2 | readonly,pubsub | O(c) channels; never fans out to other shards | differs: kevy has no RESP2 subscriber mode — after SUBSCRIBE the connection may still issue any command, where Redis restricts it to the pubsub verbs plus PING / QUIT / RESET | Subscribe to the given channels. |
| `UNSUBSCRIBE [channel [channel ...]]` | -1 | readonly,pubsub | O(c) channels | full | Unsubscribe from channels (all channels when none given). |

## script

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `EVAL script numkeys [key [key ...]] [arg [arg ...]]` | -3 | write | O(compile + the script's own work). The whole script is one atomic unit on KEYS[1]'s shard and blocks that shard for its duration. The runaway guard is an INSTRUCTION budget (lua time_limit_ms x 40000 instructions), not a wall clock | differs: there is no -BUSY state and no SCRIPT KILL — an over-budget script is aborted in place and answered with an ordinary error; nested EVAL is rejected, a redis.call touching another shard's key returns CROSSSLOT, and EVAL auto-caches its source by SHA1 so EVALSHA works without SCRIPT LOAD | Run a Lua script server-side; routed to KEYS[1]'s shard when numkeys >= 1. |
| `EVALSHA sha1 numkeys [key [key ...]] [arg [arg ...]]` | -3 | write | O(1) cache lookup, then identical to EVAL | differs: same instruction-budget model as EVAL (no -BUSY, no SCRIPT KILL); the cache is process-wide and EVAL populates it | Run a cached Lua script by its SHA1 digest. |
| `SCRIPT LOAD script \| EXISTS sha1 [sha1 ...] \| FLUSH [ASYNC\|SYNC]` | -2 | readonly | LOAD: O(len(script)). EXISTS: O(k). FLUSH: O(cached scripts) | differs: LOAD / EXISTS / FLUSH only — SCRIPT KILL does not exist, by design: there is no -BUSY state to interrupt because an over-budget script self-aborts | Manage the process-wide Lua script cache. |

## index

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `IDX.COUNT name RANGE min max \| EQ value` | -4 | readonly,extension | O(log N + matched) per shard — NOT an O(1) counter and not a B-tree rank query; it counts the matched range | kevy-only: nearest is FT.SEARCH with LIMIT 0 0 | Count index entries matching a range or equality predicate. |
| `IDX.CREATE name ON PREFIX prefix FIELD field TYPE i64\|f64\|str\|vector KIND range\|unique\|text\|ann\|agg [MAXMEM bytes] [DIM dim] [DISTANCE cosine\|l2\|ip] [M m] [EF ef] [GROUPBY field]` | -11 | write,extension | O(1) at the call; the build is asynchronous — each shard snapshots its matching keys (O(keys on that shard)) then backfills 2048 rows per tick. Per-row build cost: range/unique O(log N), text O(tokens), vector O(ef_construction * M * dim) | kevy-only: nearest is RediSearch FT.CREATE — comparable declarative index-on-prefix, but RediSearch is a separate module with its own index server; kevy's catalog is in-process and the index is maintained in the write hook | Declare a secondary index over a key prefix (catalog mutation, sidecar-persisted). |
| `IDX.DROP name` | 2 | write,extension | O(catalog size) | kevy-only: nearest is FT.DROPINDEX | Drop a declared index (catalog mutation, sidecar-persisted). |
| `IDX.LIST` | 1 | readonly,extension | Not O(1) — per shard, per index it computes live stats: range/unique O(1), text O(distinct tokens + docs), vector O(nodes + links), agg O(groups + rows) | kevy-only: nearest is FT._LIST plus FT.INFO combined | List declared indexes with per-index build state and stats. |
| `IDX.EXPLAIN name RANGE min max\|EQ value\|MATCH text\|KNN vector\|GROUPS [args ...]` | -2 | readonly,extension | The plan is parse-only, but est_rows costs the same live stats walk as IDX.LIST | kevy-only: nearest is FT.EXPLAIN, which returns a parsed query tree; ours returns kind, build state, estimated rows and a one-line plan | The IDX.QUERY parse without the execution: kind, state, est_rows, and the plan line. |
| `IDX.QUERY name RANGE min max [LIMIT n] [CURSOR c] [FIELDS field ...] \| name EQ value [LIMIT n] [CURSOR c] [FIELDS field ...] \| name MATCH text [LIMIT n] [FIELDS field ...] \| name KNN vector [LIMIT k] [EF ef] [FIELDS field ...] \| name GROUP group \| name GROUPS [BY count\|sum\|min\|max] [LIMIT n] \| HYBRID text_idx MATCH text ann_idx KNN vector [LIMIT n] [RRFK k] [EF ef] [FIELDS field ...] \| COMPOSE AND\|OR nameA RANGE min max\|EQ value nameB RANGE min max\|EQ value [LIMIT n] [CURSOR c] [FIELDS field ...]` | -4 | readonly,extension | Depends on the index kind. range: O(log N + limit). unique: O(log N + hits). full-text: O(sum of the posting lists), MaxScore-pruned. vector ANN: O(dim * (M log N + ef * M)) distance evaluations (HNSW). agg GROUPS: a bounded top-K with 1 to 3 fan-out rounds. HYBRID: both sub-queries at 4x depth, fused by RRF at the origin. COMPOSE: NOT limit-bounded — both leaves are materialised in full, because the result is key-ordered while a segment is value-ordered | kevy-only: nearest is RediSearch FT.SEARCH — genuinely comparable on two axes (our text index is BM25 over inverted segments, our vector index is HNSW, and RediSearch offers both). Differences: no separate module or index server; our BM25 statistics are per-shard, not global; and our query surface is a fixed grammar, not a DSL — no field boosting, no phrase or proximity search, no stemming, no aggregation pipeline | Query an index: range/equality, text MATCH, vector KNN, aggregation groups, or a two-index COMPOSE. |
| `IDX.REBUILD name` | 2 | write,extension | Vector indexes only (tombstone compaction). O(V_live * ef_construction * M * dim) distance evaluations, on every shard, synchronously — the most expensive verb in the extension surface | kevy-only: no Redis analogue — RediSearch handles HNSW deletes internally and exposes no rebuild verb | Rebuild an ANN index (tombstone compaction). |
| `IDX.VERIFY name` | 2 | readonly,extension | O(N) per shard — every held entry is re-read against its row and re-coerced, which is the point: it makes index drift falsifiable rather than merely asserted | kevy-only: no Redis analogue — RediSearch's index is asynchronously maintained and offers no consistency-verify verb | Verify an index: re-read every held entry against its row and report entries/bytes/coerce_failures/duplicates/drift/checked. |

## view

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `VIEW.CREATE name QUERY tree ORDER BY index [DESC] [MODE virtual\|materialized] [TOPK k] [VIA template] (tree = '( AND\|OR\|DIFF sub sub )' \| 'index RANGE min max' \| 'index EQ value')` | -8 | write,extension | O(tree size) at the call. A materialized view builds lazily on the next tick (a full eval_tree plus an O(M log M) sort); a virtual view builds nothing, ever | kevy-only: no Redis analogue. A view is neither a stored query nor a join — it is a named composition tree over declared indexes that stores membership and order only, never field values | Declare a composed view over indexes with an ORDER BY (catalog mutation, sidecar-persisted). |
| `VIEW.DROP name` | 2 | write,extension | O(catalog size) | kevy-only | Drop a declared view (catalog mutation, sidecar-persisted). |
| `VIEW.EXPLAIN name` | 2 | readonly,extension | O(sum over leaves of (log N + matched)) — each leaf's cardinality is a real count, so EXPLAIN on a broad leaf costs as much as counting it | kevy-only: nearest is FT.EXPLAIN, which returns a query tree rather than per-leaf cardinalities | Explain a view's composition tree with per-leaf cardinalities. |
| `VIEW.LIST` | 1 | readonly,extension | O(views) at the origin — the catalog is process-global | kevy-only | List declared views with their mode and shape. |
| `VIEW.QUERY name [LIMIT n] [CURSOR c] [FIELDS field ...]` | -2 | readonly,extension | virtual: O(log N_order + candidates * leaves) — it streams the ORDER index and probes membership per candidate, stopping at the limit, so it never materialises the member set. materialized: O(log M + limit). Adding FIELDS with VIA costs a SECOND fan-out to hydrate the rows | kevy-only: no Redis analogue. Absent by construction: no joins, no predicates at the view layer, no projection from the view itself, no HAVING, no aggregation | Page through a view's ordered members, optionally hydrating fields via the VIA template. |
| `VIEW.REBUILD name` | 2 | write,extension | Materialized only. A full eval_tree over the whole tree plus an O(M log M) sort and re-insert, on every shard, now (not deferred to a tick) | kevy-only | Force a rebuild of a materialized view. |
| `VIEW.VERIFY name` | 2 | readonly,extension | materialized: O(M). virtual: O(sum over leaves of (log N + leaf range) + M) — a full fresh evaluation just to report cardinality, which is the one place a virtual view is the expensive one | kevy-only | Verify a view and report member/byte statistics. |

## feed

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `FEED.READ shard generation offset [COUNT n] [PREFIX prefix ...]` | -4 | readonly,extension | A single shard (the shard is an argument, not a fan-out). O(log B) to locate the start offset in the backlog plus O(count * frame size) to decode and re-encode. COUNT defaults to 256 and is hard-capped at 4096 | kevy-only: no clean Redis analogue. Keyspace notifications are fire-and-forget with no cursor and no replay; Redis Streams have cursors but are a stream you must dual-write to, not a feed over your existing keyspace; the replication backlog is what this is built on, but Redis exposes it only to replicas. kevy is deliberately not a broker: no consumer groups, no server-side positions, no global cross-shard order | Read change-feed frames from one shard past a generation/offset cursor. |
| `FEED.SHARDS` | 1 | readonly,extension | O(1), answered locally | kevy-only: Redis has no shard-partitioned change feed to enumerate | Return the number of change-feed shards. |
| `FEED.TAIL shard` | 2 | readonly,extension | O(1) on a single shard | kevy-only: nearest is INFO replication's master_repl_offset, which is not a resumable per-shard CDC cursor | Return a shard's current feed generation and next offset. |

## migration

| Verb | Arity | Flags | Complexity | Redis | Summary |
|---|---|---|---|---|---|
| `PREFIX.STATS prefix` | 2 | readonly,extension | O(keys on the shard) — a full keyspace walk on every shard; the prefix cannot be precomputed | kevy-only: nearest is INFO keyspace's db0:keys=N,expires=M — the same two counters, but whole-DB and O(1) off a counter, where ours is per-prefix and therefore O(keyspace) | Report key-count and byte statistics for a key prefix. |
| `PREFIX.DIGEST prefix` | 2 | readonly,extension | O(keys on the shard) to walk plus O(sum of row sizes) to digest, on every shard. Per-row digests XOR together, so the result is order-insensitive and safe across shard counts | kevy-only: nearest is DEBUG DIGEST — comparable in kind (Redis also XORs per-key digests), but Redis's is whole-DB and lives behind DEBUG, while ours is per-prefix (the unit a migration actually cares about) and is a supported verb | Order-insensitive checksum of a prefix's rows for migration verification. |

