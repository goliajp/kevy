# RFC · R4c — 迁移那天(design round,零实现)

> **状态(2026-08-06 更新)**:设计轮已出 → **五个交付物全部实施完毕**
> (T1 shadow / T2 backfill-keys / T3 lint / T4 doctor / T5 sql plan)。
> **五个里有三个的原设计前提被开工时的复核推翻**(T2 的"写行"、T3 的
> "扫列看单值"、T5 的"输入格式"),都已在各自小节记下理由。四个待拍题**全部拍完**(见 §5);
> 第四题(T5 输入格式)当初推迟到开工时定,开工后发现**本题的前提是错的**
> —— 缺口不在输入格式。属主可推翻任何一条。
> **触发**:`.claude/plans/2026-08-05-v5-status.md` §二.2 —— 三件阻塞里
> 现在唯一没动的一件。R4a 自己写着 **"专业性 = 迁移那天的体验"**,而
> 第一公里是空的。

---

## 0. 一句话

**`docs/table-migration.md` 的八课是一份写得很好的需求文档,但它今天只
存在于读者的脑子里。** 本轮问的是:这八条里,**哪几条机器能替人做**、
各自的表面长什么样、哪几条**注定是清单不是工具**(说清楚比硬造工具重要)。

## 1. 今天已经有什么(先别把已有的当缺口)

| 已有 | 是什么 |
|---|---|
| `kevy-cli migrate export/import/copy-prefix` | **kevy ↔ kevy 搬数据**:遍历键空间产 rebuild 帧、断点续传(`.progress` fsync 到批)、re-prefix 拷贝 |
| `kevy-sql` | **声明期** schema 编译:SQL DDL → `TABLE.DECLARE`,不认识的东西按名字拒绝并报行列 |
| `TABLE.ENSURE` / `TABLE.REPLACE` | 第 7 课的引擎面已经齐了 |
| `TABLE.VERIFY` / `IDX.VERIFY` | 第 8 课的**数据**齐了(计数器每次调用现算、双向) |
| `IDX.ADVISE` / `AUTODECLARE` | 第 6 课的引擎面(缺路径 → 补路径,而不是复制列) |

**所以缺的不是引擎能力,是"那一天"的工序**:搬数据的那半边有了,
**定 schema、比对、切换、验收**那半边没有。

## 2. 八课逐条:机器能做到哪

| # | 课 | 机器能做吗 | 形态 |
|---|---|---|---|
| 1 | 每个查询维度在行上是不是单值 | **能,而且是事后可判定的** — 给一个候选列,扫前缀采样,报"这个维度在 N% 的行上不单值"。真实案例(thread per mailbox)的答案在 **id 构造代码**里,但**症状在数据里**:同一 item 出现在多个 owner 下 | `migrate lint`(声明前跑) |
| 2 | 每个写者在读切过去之后都是承重的 | **不能从存储判定**(这是代码审计),但**后果能测**:声明后跑影子期,盯 `missing` | 归到 #4 的影子工具 + `doctor` 的 watch 模式 |
| 3 | 回填要取"所有能点名这个 item 的来源"的**并集** | **能** — 多来源并集是纯机械活:旧索引键集 ∪ 主键空间扫描 ∪ 归档 | `migrate backfill --from …`(可多次给) |
| 4 | 切换前影子读,**内容和顺序都要比** | **能,而且这是最值钱的一件** — 跑旧路径与新路径,比集合**也比顺序**,首次分歧带**两边排序键**打一行日志 | `migrate shadow` |
| 5 | 拆旧结构:**先读者后写者** | **不能判定** — 引擎不跟踪任意键的读者。硬造一个"没人读了"的探针会给出假的安全感 | **留在清单里,并写清为什么没有工具** |
| 6 | 缺谓词 ⇒ 再加一条 ORDERPATH,**不是复制一列** | **能提示** — 同表两列在 ≥X% 行上取值相同 = 疑似复制;更正的入口本来就是 `IDX.ADVISE` | `migrate lint` 的第二条规则 |
| 7 | 用 `ensure` 开机 | **引擎面已齐**;能加的是**代码里的 spec 与库里的 spec 对不对得上**(拿 schema 文件跟 `TABLE.LIST` 比) | `migrate plan --check` |
| 8 | 把 `VERIFY` 变成运维日常,不是迁移的一步 | **能** — 计数器已经在了,缺的是**能进 cron 的外壳**:退出码、阈值、哪几个必须恒为零 | `doctor` |

**两条注定不是工具(第 2、第 5)。** 把它们写成"清单项 + 为什么机器做不了",
比造一个假探针诚实,也更有用 —— 假探针在第 5 课上尤其危险:那一课的
失败模式本来就是**静默正确地读到空**。

## 3. 提议的交付物(按价值排序,不按容易排序)

### T1 · `kevy-cli shadow` —— 影子比对 ✅ **已落地(2026-08-06)**

> **实施与设计的差异,以及为什么**:
> * 命名从 `migrate shadow` 改为顶层 `shadow` —— 它不属于"搬数据"那一组
>   (`export`/`import`/`copy-prefix` 都在动数据),它**什么都不改**,
>   只是并排读。放进 migrate 组会让人以为它有副作用。
> * **退出码即判据**:有分歧非零。切换脚本可以直接 gate,不必解析文本。
> * 两处**明确不猜**:kevy 分页回复从形状识别;但 member/score 对与平坦
>   列表**无法区分**,必须由调用方用 `--old-pairs` 声明 —— 猜错的后果是
>   把每个分数当行键、每次采样都报分歧,那种"总是在响的告警"比没有更糟。
> * 报告里写明**单次分歧是线索不是判决**(两侧背靠背读,中间写入会现形),
>   并带样本数让人读速率。
>
> **实测三种情形**:两边一致 → 0 分歧退出 0;忘掉的写者 → `MISSING` 点名
> 那一行;分数漂移 → 集合相同但 `old: u:5 (sort 5)` vs `new: u:1 (sort 10)`
> —— 正是第 4 课要的"双方排序键并排"。五个单元测试钉住契约,三语文档已写。

### ~~T1 · `migrate shadow`~~ —— 原设计(下文保留作对照)

给一对"旧问法 / 新问法",持续采样比对:

* 比 **集合** 也比 **顺序**;
* 首次分歧输出**双方的排序键**(这一行日志直接点名漂的那个写者);
* 报 `missing`(新路径没有的)与 `extra`(新路径多出来的)两侧;
* 结束时给一句可粘贴的结论:样本数、分歧数、首次分歧的键。

**为什么排第一**:它是唯一能把"我不敢切"变成"我可以切"的东西,
而且它同时兜住第 2 课(忘掉的写者会以 `missing` 现形)。

### T2 · `backfill-keys` —— 并集回填 ✅ **已落地(2026-08-06)**

> **开工前那个疑问被课文原文确认了,所以表面收窄了一半 —— 这是决定,不是缩水。**
> 第 3 课自己就切成两半:"build the backfill key-set from the **union** …
> **then write rows from the authoritative record**"。并集是机械活;而
> "权威记录是什么、一行长什么样"住在应用里 —— 一个去猜的工具会**很自信地
> 写出错的行**。所以这条命令只造键集,不写行。
> * **落成顶层 `backfill-keys`**(不在 migrate 组):它不动数据。
> * **stdout 出名字、stderr 出账目** —— 清单可以直接重定向去喂写行那一步,
>   而账目不会跟着丢。
> * **每个来源报"只有它有多少个名字"** —— 那个数就是 89%/76% 漂移在**你自己
>   数据上**的量,而不是引用别人的。
> * **读不了的来源是错误,不是空贡献** —— 静默为空的来源恰恰是这条命令要堵的洞。
> * 前缀默认剥掉(`mail:123` → `123`,好跟索引成员对齐),`--keep-prefix` 例外。
>
> 三个单元测试**不起服务**钉住"独有"的算术;两个服务端测试钉住 TYPE 分派、
> SCAN 翻页、前缀剥离与 stdout/stderr 分流。六条路径真实例实测。三语文档已写。

### ~~T2 · `migrate backfill --from <源> …`~~ —— 原设计(保留作对照)

来源可给多个:`--from-index <name>` / `--from-prefix <p>` / `--from-file <keys>`。
先求并集再写行,**并且报告每个来源各自贡献了多少独有键** —— 那个数字
本身就是 89%/76% 漂移的现场证据,值得让人看见。

> **开工前先答一个问题(T5 的教训:本题的前提可能是错的)**:"写行"要求
> 工具知道**一行长什么样**,而那是应用私有的形状(第 3 课的现场是
> `member:{owner}:{item}`)。声明一张表本来就会自动回填(`-INDEXBUILDING`
> 就是那个态),所以 T2 真正独有的价值可能只在**并集这一半** —— 报出
> "哪些键该有行、各来源各贡献多少独有键",而不是替人写它猜不出形状的行。
> 先验证这一点,再决定 T2 的表面;不要按 RFC 这段的字面直接实现。

### T3 · `lint` ✅ **已落地(2026-08-06)**· 开工前复核如下,两条都被证实

> **先验前提 —— T5 的代价还热着,那次"待拍的题"本身就是错的。**
> 下面原设计写的"给一个候选列,扫前缀采样看它单不单值"**大概率不成立**:
> 一个 hash 字段天生只有一个值,扫行永远单值,这条规则会永远报 PASS。
> 第 1 课的原话是**"答案通常在你的 id 构造 / 键构造代码里,不在行本身"**,
> 而它接着说了症状在哪:**同一个 thread 活在好几个 mailbox 里**。
> 所以真正可判定的问法是**"有没有名字出现在不止一个 owner 之下"** ——
> 输入是一族按 owner 分组的集合(前缀 `mailbox:`),输出是重叠的成员数与
> 例子。有重叠 ⇒ 没有哪一列能承载这个维度 ⇒ 该建 membership 行。
> **这恰是 `backfill-keys` 的互补题**:同一套读取机制(一族集合),
> 并集换成**交叠**。
>
> **第二条规则的时机也和标题不符**:"疑似复制列"要读**已声明表的行**,
> 只能跑在 `TABLE.DECLARE` **之后**(第 6 课本来就是"表有了、谓词缺了"的
> 场景)。所以 T3 不是一条"声明前 lint",是**两条时机不同的检查**。
> 开工时按这两点定表面,不要按下面的字面实现。
>
> **实施结果:两条复核都成立,所以 T3 落成两条命令而不是一条。**
> * `lint overlap --prefix p:` —— 读一族按 owner 分组的集合,问它们交不交叠。
>   实测复现了邮件系统原案:`t2` 同时在 `mailbox:1` 与 `mailbox:2` 下 →
>   点名那一行 + 给出 membership 行的形状 + **非零退出**(这是**答案**不是提示:
>   没有哪一列能承载点名两个 owner 的维度,声明脚本该停)。
> * `lint columns <table>` —— 跑在声明**之后**(它读行),报"在多数行上取值
>   相同"的列对。实测 `created_at` 与 `sort_ts` 在 93%(40/43)上相同 →
>   指向 IDX.ADVISE,**零退出**(这是**嫌疑**不是判决:两列合法相同是可能的)。
> * 两次读取(扫前缀 + 读集合成员)在 `backfill-keys` 已写过一遍,**第二次
>   用触发抽取** —— 移进 `collections.rs`,两边共用,行为实测未变。
>
> 四个单元测试**不起服务**钉住两条规则的算术;两个服务端测试钉住两种退出码。
> 六条路径真实例实测。三语文档已写。

### ~~T3 · `migrate lint`~~ —— 原设计(保留作对照)

* **单值性**:候选列在采样行上是否单值;不单值时直接给出
  membership-row 的形状建议(`member:{owner}:{item}`)。
* **疑似复制列**:同表两列取值高度重合 → 指向 `IDX.ADVISE`。

跑在 `TABLE.DECLARE` **之前**,因为第 1 课的原话是"先定这个,免得整张表
重新声明"。

### T4 · `doctor` ✅ **已落地(2026-08-06)**

> 计数器→退出码的映射抽成 `classify()` 一个函数,所以三个单元测试**不起
> 服务**就能把第 8 课钉住。两个刻意的选择,都不是从判据里直接读出来的:
> * **警告默认不失败**(`--warn-is-failure` 留给要严格契约的调用方)——
>   一个会因为信息而失败的 cron 很快就没人看了。
> * **`-INDEXBUILDING` 是它自己的结局,不是故障** —— 第 8 课写不出这一条
>   (那时还没有回填态),但把它当故障就会在**每次声明索引时叫人**。
>
> 四条路径真实例实测:健康 → 0;重复序值 → 警告但 0;加 `--warn-is-failure`
> → 1;索引回填中 → `BUILDING` 且 0。

### ~~T4 · `doctor`~~ —— 原设计(保留作对照)

对所有表跑 `TABLE.VERIFY`,把计数器映射成退出码:
`drift`/`missing` 非零 = 失败;`duplicates` 非零 = 警告(分页需要有界
tie-break);`absent`/`excluded`/`coerce_failures` 只报告。适合 cron。

### T5 · `sql plan` —— 第一公里 ✅ **已落地(2026-08-06)**

> **落成 `kevy-cli sql plan`,不是 `migrate plan`,理由跟 T1 出组是同一条**:
> `migrate` 那一组都在**动数据**,而 `plan` 连服务都不连 —— 它读一个文件、
> 报告、退出。更重要的是**已经有 `sql compile <file.sql>`**:`plan` 吃同一份
> 文件、来自同一个 crate,是 `compile` 的姊妹形状。放在它旁边比新造一个顶层
> 动词正统。
>
> **实现落在 kevy-sql(`plan.rs`,134 行),不在 CLI** —— "这段 SQL 在 kevy
> 里是什么意思"的属主是编译器,CLI 只渲染。划的那条语义线是这件事的全部:
> **DDL 错仍然致命**(schema 不成立就没有计划),**单条视图服务不了是一个
> 条目**。退出码:有服务不了的就非零 —— 这跟 `doctor` 的警告**不同**,
> 它不是信息:一条没有声明路径的查询**根本跑不了**,它在 schema 改掉之前
> 一直挡着这次迁移。
>
> 六个 kevy-sql 测试钉住"`compile` 停在第一条而 `plan` 不停",四个 CLI
> 测试钉住退出码契约。四条路径真实例实测(全可服务 → 0;有服务不了 → 1;
> `--apply` 被拒绝而不是被忽略;DDL 坏 → 带 line:col 的错误)。三语文档已写。

### ~~T5 · `migrate plan`~~ —— 原设计(保留作对照)

吃一份 schema(`kevy-sql` 已经能编 DDL)**加一份查询清单**,产出:
* 建议的 `TABLE.DECLARE`(列、索引、ORDERPATH);
* **查询卡片**:每条查询 → 由哪条声明路径服务;**服务不了的按名字列出来**,
  并给出"要么加这条 ORDERPATH,要么这条查询在 kevy 上不成立"的选择。

**这才是"迁移那天的体验"的正脸**:一个人拿着自己的 schema 和自己的
查询来,应该在十分钟内知道**哪些能过、哪些不能、不能的怎么办** ——
而不是读完八课自己推。

## 4. 放哪(石头/钢筋/水泥)

* T1/T2/T3/T5 的**逻辑**:钢筋 —— 认识 kevy 的领域模型(表、路径、行),
  不绑任何具体业务流。建议新 crate `kevy-migrate`,`kevy-cli` 只做外壳。
* T4 `doctor`:同上,但更薄,可以直接住在 `kevy-cli`。
* **零依赖铁律照旧**:全部 std + 自家 crate。

## 5. 四个待拍题 —— **三个已按项目原则拍板(2026-08-06),属主可推翻**

1. **范围与顺序 → 采纳 T1 → T4 → T5 → T2 → T3。**
   依据:前两个决定"敢不敢切"(没有影子比对就没人敢切,没有 doctor 就
   没人知道切完对不对),T5 决定"愿不愿来"。**T1 / T4 / T5 / T2 已完成,只剩 T3(声明前 lint)。**
2. **T5 的输入格式 → 已拍(2026-08-06,T5 开工时):不新造格式,吃一份
   SQL 文件 —— DDL 是 schema,`CREATE VIEW` 是查询。**
   依据是读代码读出来的,不是选出来的:`kevy-sql::compile` **已经**吃
   DDL 出 `TABLE.DECLARE`、吃 `CREATE VIEW` 出查询卡片、服务不了时报出
   **该加哪条 `CREATE INDEX`**。pg_dump 出的就是 DDL;而"一条有名字的
   查询"在 SQL 里的写法本来就是 `CREATE VIEW name AS SELECT …` ——
   收一份裸 SELECT 清单反而要我替每条**编个名字**。
   **顺带推翻本题的前提**:输入格式根本不是 T5 的缺口。真缺口是
   `compile()` **在第一条服务不了的视图上 `return Err`**。迁移那天要的是
   "你这 40 条查询,34 条能过、6 条不能、每条各缺什么" —— 停在第一条是
   错的形状。所以 T5 = 给 kevy-sql 加一个**不停在第一条**的 plan 入口。
   **并且划一条语义线**:DDL 错**仍然致命**(schema 不成立就没有计划可言),
   **单条视图服务不了是一个条目、不是一个错误** —— 那正是计划要报的东西。
3. **新 crate `kevy-migrate` → 暂不开。触发条件已到期,2026-08-06 复核如下。**
   **LOC 那一半真的触发了**:`shadow` 382 + `doctor` 274 + `backfill_keys` 338
   = **994 行**,远超我写的 ~400。**第二个消费者那一半没有** —— 今天只有 CLI 用。
   **仍然不抽,理由不是嫌麻烦,是那一半才是抽 crate 的正当理由**:一个新
   crate 名字在 crates.io 上是**永久的**,而且它会长在一条**已发布**的依赖链上
   (`kevy-cli` 依赖它),所以"要不要造这个公共名字"是属主的决定,不是纯技术
   决定。LOC 只是"这块该有自己的家"的代理指标,它没说这个家必须是公共的。
   **也不做把三个模块塞进一个子目录这种纯位置搬迁** —— 那是形式上的整洁,
   换不来任何东西。**现在的摆放不挡将来抽**:三个模块除了各自的 `run_*_cli`
   外壳之外没有 CLI 专属逻辑,搬走是机械动作。
   **(下方为原始记录)**
   依据:石头/钢筋/水泥的迁移由**触发**驱动,不由"看起来该抽"驱动。
   T1 落在 `kevy-cli::shadow`(逻辑 ~120 行 + CLI 外壳),一个模块撑不起
   一个 crate。**触发条件写明**:等 T2/T5 落地后,若迁移逻辑合计超过
   ~400 行、或出现第二个消费者(嵌入式/服务端也要用),再抽 crate。
4. **第 2、5 课明确不做工具 → 是决定,不是遗漏。**
   依据:第 2 课是**代码审计**(存储里没有"谁写这张表"这个事实);
   第 5 课引擎**不跟踪任意键的读者**,硬造"没人读了"的探针会给出假的
   安全感 —— 而那一课的失败模式**本来就是静默正确地读到空**,一个假探针
   会把它变成"我检查过了"。理由已写进 `docs/table-migration.md` 对应两节。

---

**原文写着"本文不动一行实现"** —— T1 落地后这句不再成立,保留在此作为
文档与代码何时分家的标记。后续交付物按 §5.1 的顺序继续。
