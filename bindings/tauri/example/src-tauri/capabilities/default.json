{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Grants the main window kevy's full command surface plus core defaults.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "kevy:default"
  ]
}
