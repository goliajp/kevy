{
  "name": "expo-kevy",
  "version": "5.3.0",
  "description": "kevy embedded in React Native via Expo Modules — the real native engine, synchronous JSI calls, one cmd() entry to all verbs. The MMKV shape (sync scalar get/set) plus everything MMKV doesn't have: TTL, structures, pub/sub, persistence you can read.",
  "license": "(Apache-2.0 OR MIT)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/goliajp/kevy.git"
  },
  "homepage": "https://kevy.golia.jp",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src",
    "ios",
    "android",
    "expo-module.config.json",
    "!android/build"
  ],
  "scripts": {
    "prepare-native": "bash scripts/prepare-native.sh"
  },
  "peerDependencies": {
    "expo": "*",
    "react-native": "*"
  }
}
