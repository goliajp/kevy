// Typecheck stub — the real module comes from the host Expo project.
// Not published (package.json "files" excludes types/).
declare module "expo-modules-core" {
  export function requireNativeModule<T>(name: string): T;
}
