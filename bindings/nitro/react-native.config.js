// Consumed by React Native CLI autolinking so this Nitro module is picked
// up by the app's Android build.
module.exports = {
  dependency: {
    platforms: {
      ios: {},
      android: {},
    },
  },
}
