/// <reference types="vite/client" />

// Injected by vite.config.ts from the workspace Cargo.toml. Declared here
// so the type checker knows it exists and that nothing may assign to it.
declare const __KEVY_VERSION__: string
