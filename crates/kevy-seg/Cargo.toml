[package]
name = "kevy-seg"
description = "Immutable ordered-record segment files: build once, binary-search forever. The cold-tier storage stone."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
documentation = "https://docs.rs/kevy-seg"
readme = "README.md"
keywords = ["segment", "immutable", "binary-search", "storage", "crc32c"]
categories = ["database-implementations", "data-structures"]
homepage.workspace = true
authors.workspace = true

[dependencies]
kevy-sys = { path = "../kevy-sys", version = "6.4.0" }

[dev-dependencies]
kevy-tmpdir = { path = "../kevy-tmpdir", version = "6.4.0" }

[lints]
workspace = true
