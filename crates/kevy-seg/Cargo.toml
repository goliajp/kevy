[package]
name = "kevy-seg"
description = "Immutable ordered-record segment files: build once, binary-search forever. The cold-tier storage stone."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
kevy-sys = { path = "../kevy-sys", version = "4.1.1" }

[dev-dependencies]
kevy-tmpdir = { path = "../kevy-tmpdir", version = "4.1.1" }
