# kevy-madvise audit — 2026-05-27

First STONE-AUDIT for `kevy-madvise`, carved out of `kevy-sys` on this
date per `KEVY-SYS-VERDICT-2026-05-27.md`. The content was previously
covered by `kevy-sys`'s CEMENT-AUDIT (single-file `advise_hugepage`
wrapper); now that the function lives in a generic, publishable stone it
graduates to STONE-AUDIT criteria.

## T1 (GATE)

- [x] identity: "Pure-Rust madvise hints (MADV_HUGEPAGE) — no libc crate." (57 chars)
- [x] missing-docs: 0 (single `pub fn`, full rustdoc + Safety section in
      crate doc)
- [x] cargo test: 4 / 4 pass (under-threshold / unaligned / zero-length /
      large-aligned)
- [x] clippy `-Dwarnings`: 0 findings
- [x] cargo doc: 0 warnings
- [x] unsafe SAFETY comments: 1 / 1 block
- [x] 0 third-party deps (`[dependencies]` empty)
- [x] charter: extern "C" only in the OS-boundary crate series
      (kevy-sys / kevy-uring / kevy-madvise) — confirmed by grep

## T2 (BAR)

- [x] line coverage: tests cover every branch of the page-alignment
      math (under-threshold short-circuit, unaligned start, zero-length,
      aligned-large). llvm-cov number not measured because the entire
      Linux-only branch is dark on macOS; visual coverage of every
      branch confirmed.
- [x] miri / loom: NOT REQUIRED (syscalls bypass both — same rule that
      applied when the function lived in `kevy-sys`).
- [x] `benches/`: not present; the function is one syscall + integer
      math, with no per-call hot path to bench independently of the
      kernel's `khugepaged`. Caller-side perf gain is asserted in
      `kevy-map`'s benches (where the hint was originally introduced).
- [x] perf gate test: N/A (best-effort kernel hint — no return value to
      gate against).
- [x] LOC: ~95 (well under 1000)
- [x] `cargo package --list` clean (verified via Phase D smoke pattern;
      to be re-run as part of v0.publish dry-run refresh)

## T3 (PUB)

- [x] CHANGELOG.md: present, 0.1.0 entry
- [x] PLATFORMS.md: present (Linux Tier 1; Darwin / other Tier 2-3
      no-op)
- [x] MEM-BUDGETS.md: present (0 heap)
- [x] Cargo.toml metadata: description, keywords (5), categories,
      readme, license `Apache-2.0 OR MIT`, repository, homepage,
      documentation per-crate
- [x] LICENSE-APACHE + LICENSE-MIT files present

## Verdict

GATE: ✅ pass; BAR: ✅ pass; PUB: ✅ ready.

This is the first audit after the kevy-sys split; nothing pre-existing
was relaxed.
