[package]
name = "kevy-chaos"
description = "Chaos test harness for kevy — spawns kevy as a child, drives concurrent writes, simulates crashes, verifies invariants. Test-only; not for production server inclusion."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
# Test-only crate. Not published to crates.io.
publish = false

[lints]
workspace = true

# 0-dep: std-only.
[dependencies]
# For free_port. This crate had its own, and the two answered the same
# question differently — see pick_free_port.
kevy-testnet = { path = "../kevy-testnet" }
