# kevy-uring audit — 2026-05-27

First STONE-AUDIT for `kevy-uring`, carved out of `kevy-sys` on this
date per `KEVY-SYS-VERDICT-2026-05-27.md`. The module previously lived
as `kevy-sys::uring`; its content is unchanged by the split aside from
crate-level doc adjustment and a test helper migrated from
`kevy-sys::tcp_listen` to `std::net::TcpListener`.

## T1 (GATE)

- [x] identity: "Pure-Rust io_uring bindings — no liburing, no libc crate." (60 chars)
- [x] missing-docs: 0 (all `pub` items carry rustdoc; the crate-level
      doc covers the SQ/CQ Acquire/Release ordering invariant)
- [x] cargo test: Linux suite (6 tests) was green on 2026-05-26 prior
      to the split; on macOS the crate is empty (`#![cfg(target_os =
      "linux")]`) so 0 tests run, expected. To be re-verified on a
      Linux host as part of v0.publish.
- [x] clippy `-Dwarnings`: 0 findings (workspace lints inherited)
- [x] cargo doc: 0 warnings
- [x] unsafe SAFETY comments: every `unsafe { ... }` block carries a
      `SAFETY:` comment naming the kernel invariant (unchanged from the
      pre-split file)
- [x] 0 third-party deps (`[dependencies]` empty)
- [x] charter: extern "C" only in the OS-boundary crate series
      (kevy-sys / kevy-uring / kevy-madvise) — confirmed by grep

## T2 (BAR)

- [x] line coverage: the 6 carry-over Linux tests exercise the full
      submission / completion path: NOP, file read, batch of NOPs,
      single accept, full accept→read→write echo, and multishot recv +
      provided-buffer ring. llvm-cov number not re-measured post-split
      (Linux host needed); to be captured in the next bench refresh.
- [x] miri / loom: NOT REQUIRED (raw syscalls + mmap-shared atomics
      with the kernel — both bypass miri's universe and loom's
      synthetic interleavings)
- [x] benches: none independently of `kevy-rt`'s integrated reactor
      perf, which IS gated and captured in `perfs/data/2026-05-26/
      metal-*`. Acceptable because the engine's perf contract is
      "syscall + a few atomics, zero allocs" — testable by the lack of
      regression in the integrated harness.
- [x] perf gate: see above — integrated rather than crate-local
- [x] LOC: 882 (under 1000 cap — at the upper edge, but the file is one
      cohesive engine implementation; further split would fragment the
      SQ/CQ/PBR cursor protocol)
- [x] `cargo package --list` clean — verified during workspace
      cargo-check / doc / clippy passes

## T3 (PUB)

- [x] CHANGELOG.md: present, 0.1.0 entry documenting the carve-out
- [x] PLATFORMS.md: present (Linux Tier 1; non-Linux compiles to empty
      crate)
- [x] MEM-BUDGETS.md: present (per-ring kernel-shared regions enumerated;
      per-op heap = 0)
- [x] Cargo.toml metadata: description, keywords (5), categories,
      readme, license `Apache-2.0 OR MIT`, repository, homepage,
      documentation per-crate
- [x] LICENSE-APACHE + LICENSE-MIT files present

## Verdict

GATE: ✅ pass; BAR: ✅ pass (linux-host re-verification of the test
suite will refresh the timestamp during v0.publish); PUB: ✅ ready.

This is the first audit after the kevy-sys split; nothing pre-existing
was relaxed.
