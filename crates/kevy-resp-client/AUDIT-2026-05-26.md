# kevy-resp-client audit — 2026-05-26

Audited per [`STONE-AUDIT.md`](../../STONE-AUDIT.md). Identity: "Blocking
RESP2 client over TCP". Freshly carved out of kevy-cli on this same day
(per the stone/cement reclassification — kevy-cli became a dev tool).

## T1 (GATE)

| # | Criterion | Status | Detail |
|---|---|---|---|
| 1.1 | Identity ≤ 60 chars | ✅ | 42 chars: "Blocking RESP2 client over TCP. Pure Rust." |
| 1.2 | missing_docs = 0 | ✅ | 0 (added `#![warn(missing_docs)]` from carve-out; all pub items doc'd) |
| 2.1 | `cargo test -p kevy-resp-client` | ✅ | 7 roundtrip + 1 doctest pass |
| 2.2 | `cargo clippy -Dwarnings` | ✅ | 0 |
| 2.3 | `cargo doc` 0 warnings | ✅ | fixed broken intra-doc link `[kevy-resp]` → `[kevy_resp]` |
| 3.1 | unsafe scope | ✅ | `#![forbid(unsafe_code)]` |
| 3.2 | SAFETY: comments | N/A | no unsafe |
| 7.1 | 0 third-party deps | ✅ | only path-dep `kevy-resp` |
| 6.1 | Endian/arch declared | ✅ | RESP is byte-oriented, endian-agnostic |
| 8.1 | Workspace inherits | ✅ |

**T1 verdict: PASS** (1 broken-doc-link fixed.)

## T2 (BAR)

| # | Criterion | Status | Detail |
|---|---|---|---|
| 2.4 | Line coverage ≥ 90% | ✅ | **90.91%** lines / **100%** functions / 87.10% regions (was 0% — only doctest, which was `no_run`) |
| 2.5 | Pub API doctest/test | ✅ | 7 roundtrip tests cover both pub methods (`connect`, `request`); doctest demonstrates basic usage |
| 3.3 | miri | ⚠️ | NOT RUN — would require miri to model `std::net::TcpListener` + thread, which is heavy (and last attempt on similar TCP code failed with "operation not supported"). The blocking I/O path has minimal logic in client code; miri's value here is low. |
| 3.4 | loom | N/A | single-thread blocking client; no cross-thread state |
| 3.5 | fuzz | ⚠️ | the client itself doesn't parse — it delegates to `kevy_resp::parse_reply`. Fuzzing belongs to kevy-resp (Phase A #4). Document as such. |
| 4.1 | Perf contracts | ⚠️ | not explicit; the client is I/O-bound (TCP round-trip dominates) — perf is mostly the kernel's, not ours. Document. |
| 4.2 | size_of const-asserts | N/A | not a layout-critical type |
| 4.3 | benches/examples | ⚠️ | DEFERRED — bench would mostly measure TCP loopback + parse, not the client; the kevy server's metal_keyspace.sh covers end-to-end |
| 4.4 | Baseline-vs-std | N/A | std has no RESP client |
| 4.5 | `tests/perf_gate.rs` | N/A | I/O-bound; per-op ns is dominated by network, not by our code |
| 5.1 | size_of pub types | N/A |
| 5.2 | alloc-count | ⚠️ | `request` allocates a fresh `Vec<u8>` (encode buffer) per call. Could be reused via a `request_into(&mut Vec<u8>, ...)` API. Tracked for future. |
| 5.3 | Drop leak-free | ✅ | `TcpStream` + `Vec<u8>` both safe-drop; no manual resources |
| 7.2 | LOC ≤ 1000 | ✅ | **83** (smallest stone) |
| 7.3 | cargo package bytes ≤ 50 KB | ✅ | tiny |

**T2 verdict: PASS with documented N/A and ⚠️ deferrals**:
- bench / perf_gate: not applicable to an I/O-bound client (TCP latency
  dominates; perf budget doesn't fit the model)
- fuzz: covered by kevy-resp (the actual parser)
- miri: limited model coverage on real TCP; manual reasoning + 7
  roundtrip tests are the substitute
- alloc-count: noted as a future API extension (`request_into`) for
  callers that need zero-alloc per request

7 roundtrip tests (NEW this audit) cover: ping/pong, GET → bulk,
GET → nil, INCR → int, KEYS → array, error reply, partial-close →
UnexpectedEof.

## T3 (PUB) — deferred per POLISH-PLAN Phase C

All not done yet; not blocking.

## Verdict

- **GATE (T1)**: ✅ pass
- **BAR (T2)**: ✅ pass (with explicit N/A / deferral rationale for the
  bench/fuzz/miri criteria that don't fit a TCP client)
- **PUB (T3)**: ⏸ deferred

This is the smallest, newest stone (83 LOC, carved this session). The
audit closed: the 0% coverage gap (now 90.91%), the doc broken-link, and
formally documented why the "bench / perf_gate / fuzz" criteria don't
apply to an I/O-bound client wrapper.
